# Request Contract

Require `request_id`, title, audience, decision, template family, grain, freshness,
dashboard filters, KPI IDs, acceptance examples, and `open_questions`.

Use an empty `open_questions` list only when the request can enter KPI resolution.
For a working example, read `examples/DB-104/request.yaml`.
